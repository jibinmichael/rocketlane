# fixtures/

Datasets and scenarios as data. Nothing in `core/` imports these files; they enter the system only through `core/ingestion`, exactly like a CSV uploaded through "Test any project files".

Each fixture folder has a `README.md` stating what it proves. Planned set and purpose: see [docs/agent-context/07-data-contract.md](../docs/agent-context/07-data-contract.md).
