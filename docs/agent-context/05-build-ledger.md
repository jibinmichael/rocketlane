# 05 — Build ledger

Locked build order (spec §50) with gates (spec §40). Update after every work session. Status: `todo · in-progress · passed · blocked`. A step is `passed` only when its gate command output has been pasted or summarised truthfully here.

Gate command for every step from 03 onward: `pnpm typecheck && pnpm lint && pnpm test`. Steps with UI additionally require a manual browser check noted in "Evidence".

| # | Step | Gate | Status | Evidence / notes |
|---|---|---|---|---|
| 01 | Inspect existing code | audit report exists | **passed** | `docs/audit/2026-09-16-current-state-report.md` |
| 02 | Establish clean project boundaries | `git init`; `core/`, `fixtures/`, `tests/` created; lint boundary rule active; vitest runs; dead scaffold removed; package renamed; `.env.example` has `ANTHROPIC_API_KEY=` | **passed** | 2026-09-16: typecheck OK · lint OK · test OK (1 file, 1 test, type errors none) · format OK · build OK (routes `/`, `/_not-found`). Boundary probe: react / `@/core/execution` / `../` imports inside `core/agent` → 3 errors; `@/core/mission` + `@/lib` inside `core/domain` → 2 errors; allowed imports in `core/execution` → 0. First attempt missed `react` because per-module overrides replaced the base rule; fixed by composing base + module rules. Deps added: zod 4.6, @anthropic-ai/sdk 0.126, vitest 5.0, @vitest/coverage-v8 5.0. |
| 03 | Domain model (+ ingestion of shape A, pulled forward from 18) | Gate 1: same code builds `WorkspaceGraph` from `rocketlane-export` and `cascading-conflicts` fixtures; unit tests on indexes | **passed** | 2026-09-16: 19 tests pass. Real export: 31 projects, 325 tasks, 20 phases, 9 milestones, 39 subtasks, 53 predecessor edges, 0 rejected; findings surfaced: DUPLICATE_NAME (TSK-0312/0313), 76 HISTORICAL_POLICY_INCONSISTENCY, 28 PROJECT_WITHOUT_TASKS, no cycles. Hero fixture builds Go-Live → Deploy API → QA Complete from data. Longest-match dependency resolution tested incl. comma-containing names; first DP tiebreak swallowed a known name into an unknown fragment, fixed to minimise unresolved parts. |
| 04 | Governance engine | Gate 2: each of the four policies blocks and allows deterministically; evidence recorded in `PolicyEvaluation`; snapshot tests | **passed** | 2026-09-16: policies as declarative trigger → validations objects with versions; `evaluateGovernance` returns per-policy `triggerMatched`, validations, blocking reasons, evidence; data flags (unresolved dependency, cycle) also block. NA interpretation switch tested both ways. Role-based permission boundary with escalation tested (owner / assigned member / denied). |
| 05 | Dependency resolver | Gate 3: arbitrary chains traversed; shortest useful path; cycle detection; comma-names resolve by longest match and fail closed | **passed** | 2026-09-16: target resolution (exact > normalized > prefix > token) with ambiguity → clarification, tested on hero and on the real export's duplicate name; `traceCurrentBlockers` walks failing policy evidence to the nearest actionable node (hero: Acme → Go-Live → Deploy API → QA Complete, log_time); `resolveClosure` yields dependency-ordered required transitions and open-but-not-required tasks (D-07). Real export PRJ-013 traversed with no names in code. Property test on random DAGs deferred (time). |
| 06 | Mission engine | Gate 4: mission state persists through `MissionStore`; browser reload check at step 12 | **passed (core)** | 2026-09-16: `Mission` aggregate with plan steps (stable ids = idempotency keys), pending decisions as mission state (never UI), decisions, state-change notices, exact outcome. `MemoryMissionStore`; `LocalStorageMissionStore` arrives with the UI. |
| 07 | Execution engine | Gate 5: `ProposedPlan` rejected at type level; writes precondition/permission/policy checked and verified; timeout → reconcile → same-key retry | **passed** | 2026-09-16: `validateFlightPlan` is the only producer of the branded `FlightPlan` (module-private symbol); type test passes. Engine: already-satisfied skip, permission before policy before write, HIGH_IMPACT waits as `WAITING` mission state, write → fresh-read verify, `timeout_once` reconciled by re-read (applied → VERIFIED, 0 retries, 1 ledger entry), `fail_once` retried once with the same key. Hero journey lands end to end; declined confirmation leaves the project open; already-complete project writes nothing. Reload-with-RUNNING reconciliation deferred to step 12 (needs persistence). |
| 08 | Revalidation | Gate 6: world change pauses and replans; own writes ignored; stale plan never executes | **passed (core)** | 2026-09-16: external write inside the closure → `STATE_CHANGED` → `MISSION_PAUSED` → replan → `STALE` with a state-change notice; own correlation id and out-of-closure changes ignored; run loop stops on STALE and merges in-flight step results instead of overwriting the pause; `resume` re-derives the closure. Cancel and change-scope (exclusion prunes the subtree; verified state never reverted) tested. BroadcastChannel arrives with persistence at step 12. |
| 09 | Event / activity model | event log drives an activity projection and audit record; tests | **passed (core)** | 2026-09-16: 23 event types (§36), append-only `EventLog` with subscribe, `auditFor` projection (who/what/why/when/result/verified). Every `ACTION_COMPLETED` carries `verified: true`; asserted in the hero test. Activity UI at step 12+. |
| 10 | Conversational state layer | renderer emits blocks for every mission state; copy tests; deterministic interpreter + grounding; model interpreter server action (time-boxed at hour 7) | **passed (core)** | 2026-09-16: closed intent union with zod schema, span-only proposals, deterministic grounding (ambiguity → clarification, not found → boundary, malformed → unsupported); grammar covers complete/log time/leave open/stop/continue/yes/no/why/path/status/all projects; world knowledge locked (tests). Renderer: 26 block types over `Inline` slots (entity/policy/count/time chips), progressive order asserted on hero, confirm-with-consequence, timeout beat, landing, course correction, batch summary with non-zero buckets. Model adapter pending (hour 7). |
| 11 | Reusable artifact system | one interaction contract, several presentations | **passed (compressed)** | 2026-09-16: `Block` + `Inline` contract (entity/policy/count/time slots, actions with impact, expandable detail, path) rendered by `ConversationBlockItem`; `ArtifactStateChip`, `ArtifactMissionRow`, `MissionPathList`. Dev-only artifacts tab not built (time). |
| 12 | Core conversation journey | Gate 7: hero completes inside the conversation; browser check | **passed** | 2026-09-16: headless runtime test + CDP screenshots of the live thread (blocked → chain → log time inline → verified × 6 → confirm with consequence → landed). Real-export journey exercised via the batch scenario and Projects → Open in agent; PRJ-028 single-project browser walk not screenshotted (time). |
| 13 | Mid-flight change | course-correction journey passes in browser and as a scenario | **passed** | 2026-09-16: scenario `course-correction` + live thread screenshot: paused, what changed / what it affects, continue, replanned 5 of 6, landed. Two-tab BroadcastChannel path wired; manual browser check listed in the demo script. |
| 14 | User interruption | cancel + change-scope journeys pass in browser and as scenarios | **passed (scenarios + headless runtime)** | 2026-09-16: `user-interruption-cancel`, `scope-change-leave-open`; runtime test for "actually leave Handover open" (verified state reported, never reverted); Esc/Stop in composer. |
| 15 | Partial success | "Complete all projects" on real export yields exact breakdown | **passed** | 2026-09-16: scenario `batch-real-export` (Robert Oconnell: 1 completed, 1 blocked, 29 not permitted → PARTIALLY_COMPLETED) + renderer test for the non-zero-bucket summary with per-target detail. |
| 16 | Routine checks | designed (D-24) | **not built** | Documented in README known limitations; `create_routine` intent answers with a boundary block that says so. |
| 17 | Conversational notifications | designed (D-24) | **not built** | Home inbox with needs-input-first ordering exists; routine-driven notifications do not. |
| 18 | CSV ingestion | Gate 8: uploaded data drives the system; report lists rejects/warnings/findings | **passed (shape A)** | 2026-09-16: browser upload in the Lab (parsed client-side), report with counts/rejects/warnings/findings. Five-file brief shape (B) **not built**; documented. |
| 19 | Test Lab | upload → ingest → run scenarios → results; failure injection | **passed** | 2026-09-16: Dataset / Scenarios / World tabs; Run all 11; weaken any of the four policies; regression records; `?run=` deep link plays a scenario in the live conversation; world mutator + fault arming. Scenario authoring UI not built (scenarios are JSON in `core/evaluation/scenarios.ts`). |
| 20 | Evaluation engine | machine-checkable assertions, score, version stamps | **passed** | 2026-09-16: write observer judges every write against the reference policies, permissions, scope closure and stale state; outcome + final-state assertions; agent/policy/dataset/eval version stamps. Compaction scenario not built. |
| 21 | Regression suite | Gate 9: a recorded failure replays and is caught | **passed (mechanism)** | 2026-09-16: weakened-engine test is the reproduced failure; Lab emits regression records with diagnosis; `tests/regression/` folder exists, no records committed yet. |
| 22 | Adversarial tests | misleading content, out-of-scope, ambiguity | **passed (deterministic)** | 2026-09-16: instruction-like task name is data; out-of-scope → boundary; ambiguity → clarification; malformed proposals rejected by schema. Model-side adversarial run and `interpreter_disagreement` not built. |
| 23 | Motion / streaming | state transitions per token table; reduced-motion | **passed (compressed)** | 2026-09-16: motion tokens in CSS + `lib/motion.ts`; block settle, capped reveal stagger (6 × 60ms), hairline progress (indeterminate/determinate), chip crossfade; `prefers-reduced-motion` global. Path-contraction animation not built. |
| 24 | Visual polish | Gate 10: tokens applied; accent chosen | **passed (compressed)** | 2026-09-16: status/state/shadow/motion tokens; body background via token; Geist removed; restrained indigo accent for the active nav only; headless screenshots of all surfaces reviewed. Dark mode tokens defined, not reviewed. |
| 25 | Final verification | clean build, tests, README, no secrets, demo runs from documented commands | **passed** | 2026-09-16: `pnpm build` (7 routes), `pnpm test` 89/89, typecheck, lint, format; secret scan clean; README + demo script + architecture overview + test results written. |

## 8-hour compression (decided 2026-09-16, submission deadline)

Order unchanged; depth reallocated. Steps 03+18 (domain + ingestion) merge because the domain gate needs real data. Steps 16–17 (routines, notifications), the scale generator and replay cassettes are **documented, not built**, and listed as known limitations in the README. The model interpreter is time-boxed to 30 minutes at hour 7; if it slips, the deterministic interpreter ships and the seam is documented. Depth goes to the conversation (hero + four edge journeys) and the Test Lab.

## Session log

| Date | Steps touched | Summary | Left out deliberately |
|---|---|---|---|
| 2026-09-16 | 01 | Read-only audit, agent-context docs, implementation plan, proposed ADRs, fixtures copied; adversarial review folded in; human resolved all open questions (model interpreter approved) | No source changes; no dependencies installed; no git init |
| 2026-09-16 | model | Key added by the human (moved out of a tracked file before any commit); live model interpreter verified in the UI ("Interpreted by model"); fixed empty-env model id, strict-schema maxItems, span snapping, name-coverage grounding; home composer shows interpreting state | Replay cassettes and interpreter_disagreement metric still not built |
| 2026-09-16 | 10–25 | intent layer, renderer, runtime, conversation UI, projections, evaluation engine, Test Lab, model interpreter action, docs; 89 tests; production build green | Routines/notifications, five-file ingestion shape, scale generator, replay cassettes, model-side adversarial run, scenario authoring UI, dark-mode review. Fixed during gates: brand symbol, pending not cleared, subtree pruning, run-loop race, resolver-independent policy weakening, stale CSS chunk. |
| 2026-09-16 | 03–09 | domain, ingestion, governance, resolver, system of record with faults, mission engine, executor, revalidation, event log; 55 tests | UI, persistence, BroadcastChannel, routines. Fixed during gates: DP tiebreak swallowing known names; per-module lint override replacing base rule; runtime-less brand symbol; pending not cleared after input; exclusions not pruning subtrees; async change handling racing the run loop (now merged). |
| 2026-09-16 | 02 | git init; deps; `core/` `tests/` `fixtures/` READMEs; lint boundary (base + intra-core) verified by probes; vitest config with type-tests; removed `app/c`, `components/chat`, `HANDOFF.md`, starter SVGs, unused Geist fonts; package renamed; CLAUDE.md addendum applied (approved); README rewritten; `.env.example` extended; CI test step | No `core/` code yet (step 03). Interim `app/page.tsx` shell exists only so the build passes; replaced at step 12. `types/` folder kept until step 25 decision. |
