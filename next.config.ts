import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // The dev indicator badge overlaps the "Acting as" footer during demos; nothing else changes.
  devIndicators: false,
}

export default nextConfig
